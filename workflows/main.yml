# name: Node.js CI

# on:
#   push:
#     branches: [master]
#   pull_request:
#     branches: [master]

# jobs:
#   build:
#     runs-on: self-hosted
#     steps:
#       - uses: actions/checkout@v2
#       - name: Use Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v1
#         with:
#           node-version: ${{ matrix.node-version }}

#       - run: npm i
#       - run: npm start

#       - run: |
#           pm2 stop animal_bombers
#           pm2 start animal_bombers
#           pm2 save
#           sudo service nginx restart
#           pm2 resurrect
